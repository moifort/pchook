mutation UpdateBook($id: BookId!, $input: UpdateBookInput!) {
  updateBook(id: $id, input: $input) {
    id
    title
    authors
    publisher
    genre
    status
    updatedAt
  }
}

mutation DeleteBook($id: BookId!) {
  deleteBook(id: $id)
}

mutation AddToFavorites($id: BookId!) {
  addToFavorites(id: $id) {
    id
    status
  }
}

mutation AddReview($bookId: BookId!, $input: CreateReviewInput!) {
  addReview(bookId: $bookId, input: $input) {
    bookId
    rating
    readDate
    reviewNotes
    createdAt
  }
}

mutation RefreshBook($id: BookId!) {
  refreshBook(id: $id)
}
